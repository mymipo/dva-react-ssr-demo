import React from 'react';
import { Link } from 'dva/router';
import { Layout, Menu } from 'antd';

const { Header } = Layout;

function IndexPage() {
  return (
    <div>Home Contect</div>
  );
}

IndexPage.propTypes = {
};

export default IndexPage;
